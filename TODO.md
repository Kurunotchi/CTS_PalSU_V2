# Capacity Testing Station Web Dashboard Fix

## Task: Fix "cant connect to html website"

### Approved Plan Steps:
- [x] 1. Create src/public/ directory 
- [x] 2. Place static files in src/public/: index.html, app.js, style.css
- [ ] 3. Install Node.js deps and start server 
- [ ] 4. Open http://localhost:3000/dashboard in browser

**Status**: Static files served correctly from public/. Run `src/start.bat` next to start dashboard server (installs express/ws if needed).

